// module.exports = {
// boughtBy: async (course, args, { models, user }) =>
//     await models.User.findById(course.instructor)
//         .sort({ _id: -1 })
//         .exec()
// };
module.exports = {
    // instructor: async (course, args, { models }) => {
    //     return await models.Instructor.findById(course.instructor)
    //         .sort({ _id: -1 })
    //         .exec();
    // }
    // boughtBy: async (course, args, { models, user }) =>
    // await models.User.find({_id:{$in:}})
    //     .sort({ _id: -1 })
    //     .exec()
};
